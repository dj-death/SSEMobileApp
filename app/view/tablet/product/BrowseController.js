Ext.define('App.view.tablet.product.BrowseController', {
    extend: 'App.view.product.BrowseController',
    alias: 'controller.tablet-productbrowse',

    /*onCreate: function() {
        var record = Ext.create('App.model.Product');

        Ext.create({
            xtype: 'productcreate',
            record: record,
            centered: true,
            floated: true,
            modal: true,
            ui: 'dialog',
            toolbar: {
                docked: 'bottom'
            }

        }).show();
    }*/

});
