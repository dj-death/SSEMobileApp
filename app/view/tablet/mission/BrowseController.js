Ext.define('App.view.tablet.mission.BrowseController', {
    extend: 'App.view.mission.BrowseController',
    alias: 'controller.tablet-missionbrowse',

    /*onCreate: function() {
        var record = Ext.create('App.model.Mission');

        Ext.create({
            xtype: 'missioncreate',
            record: record,
            centered: true,
            floated: true,
            modal: true,
            ui: 'dialog',
            toolbar: {
                docked: 'bottom'
            }

        }).show();
    }*/

});
